services:
  gloci:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gloci
    ports:
      - "8080:8080"
    environment:
      GLOCI_PORT: "8080"
    restart: unless-stopped
